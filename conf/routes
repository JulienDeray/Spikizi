# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Dashboard
GET         /                        controllers.Application.index
GET         /dashboard               controllers.Application.dashboard

## JS
GET         /websocket               controllers.Application.webSocket
GET         /js/interpretedJS        controllers.Application.interpretedJS()

POST        /authentification        controllers.Application.authentification

# Mobile
GET         /m                       controllers.Application.mobile
GET         /m/user/:name            controllers.Application.mobileSpeak(name)
GET         /m/logout                controllers.Application.logout

POST        /m/newUser               controllers.Application.newUser

# WebSocket
GET         /sse                     controllers.Pusher.sse()

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file            controllers.Assets.at(path="/public", file)
