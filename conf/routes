# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Dashboard
GET         /                        controllers.Dashboard.dashboardLogin
GET         /dashboard               controllers.Dashboard.dashboard
GET         /dashboard/logout        controllers.Dashboard.dashboardLogout

## JS
GET         /websocket               controllers.Dashboard.webSocket
GET         /js/interpretedJS        controllers.Dashboard.interpretedJS()

POST        /authentification        controllers.Dashboard.authentification

# Mobile
GET         /m                       controllers.Mobile.mobile
GET         /m/user/:name            controllers.Mobile.mobileSpeak(name)
GET         /m/logout                controllers.Mobile.logout

POST        /m/newUser               controllers.Mobile.newUser

# WebSocket
GET         /sse                     controllers.Pusher.sse()

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file            controllers.Assets.at(path="/public", file)
